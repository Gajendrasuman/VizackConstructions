import { Teko } from "next/font/google";

export const teko = Teko({
    weight: ["700"],
    subsets: ["latin"]
  })

export  const montserrat = Teko({
    subsets: ["latin"],
    display: "swap",
    weight: ["700"]
})